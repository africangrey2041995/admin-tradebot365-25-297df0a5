
// Re-export the useSignalFilters hook from the new location
export { useSignalFilters } from '@/components/signals/hooks/useSignalFilters';
// Properly re-export the type from the central types file
export type { SignalFilters } from '@/components/signals/types';

