// This is a placeholder until we implement real auth
export const useAdmin = () => {
  return {
    isAdmin: true, // Always return true for now
    isSuperAdmin: true, // Always return true for now
  };
};
