
// This is a barrel file to maintain backward compatibility
// It re-exports the hook from its new location

import { usePremiumBotDetail } from './premium-bot/usePremiumBotDetail';

export { usePremiumBotDetail };
